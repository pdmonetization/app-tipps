{
  "name": "app-tipps",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "predev": "node scripts/generate-responsive-images.mjs",
    "dev": "astro dev",
    "prebuild": "node scripts/generate-responsive-images.mjs",
    "build": "astro check && astro build && node scripts/generate-github-pages-redirects.mjs && node scripts/check-seo-build.mjs && node scripts/check-internal-links.mjs && node scripts/check-routing.mjs && npm run check:worker",
    "preview": "astro preview",
    "check": "astro check",
    "score:content": "node scripts/score-content.mjs",
    "check:seo": "node scripts/check-seo-build.mjs && node scripts/check-internal-links.mjs && node scripts/check-routing.mjs",
    "check:worker": "node --test worker/test/*.test.mjs && node scripts/check-worker.mjs",
    "deploy:worker": "wrangler deploy --config worker/wrangler.jsonc"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.11",
    "@astrojs/sitemap": "^3.2.1",
    "astro": "^5.1.1",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "ajv": "8.20.0",
    "esbuild": "0.27.7",
    "typescript": "^5.9.3",
    "wrangler": "4.125.0"
  }
}
